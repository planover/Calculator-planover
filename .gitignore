# ── Rust 构建产物（engine/target 约 488MB，绝不入库）──
engine/target/
**/*.rs.bk

# ── Dart / Flutter ──
app/.dart_tool/
app/.packages
app/.pub-cache/
app/.pub/
app/build/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/android/.gradle/
app/android/local.properties
app/android/.idea/
app/android/key.properties
app/android/**/*.jks
app/android/**/*.keystore
*.apk
*.aab
# CI 由 flutter create 生成后再拷回的 gradle 壳（二进制 wrapper 不入库，见 ARCHITECTURE §2.1）
app/android/gradle/wrapper/gradle-wrapper.jar

# ── IDE / 编辑器 ──
.idea/
.vscode/
*.iml

# ── 操作系统 ──
.DS_Store
Thumbs.db
desktop.ini
